# Task 2 Report: Update package.json

**Status:** DONE

**Commits:**
- `305d868` build: update package.json for Vite 8 (remove umi, add vite)

**Verification:** Rewrote `web/package.json` — removed `umi` and `@umijs/plugins`, changed scripts to `vite`/`vite build`, added `vite`, `@vitejs/plugin-react`, `less`, `typescript` as devDependencies. File content matches the task brief exactly.

**Concerns:** None.
